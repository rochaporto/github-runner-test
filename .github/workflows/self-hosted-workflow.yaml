name: self-hosted-workflow
on:
  workflow_dispatch:

jobs:
  test-cvmfs:
    runs-on: arc-rochaporto
    steps:
      - name: test cvmfs
        run: ls /cvmfs/atlas.cern.ch/repo
  test-gpu:
    runs-on: arc-rochaporto-gpu
    container: rochaporto/gpu_burn
    steps:
      - name: gpuburn
        run: |
          nvidia-smi
          ./gpu_burn 60
